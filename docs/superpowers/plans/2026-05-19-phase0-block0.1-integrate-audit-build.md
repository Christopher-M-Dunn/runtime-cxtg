# Phase 0 Block 0.1 — Integrate, Audit, Build, and Boot Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Confirm Artur's CX commits are cleanly integrated into `qemu-cxtg:cxtg-dev`, produce a verified clean build, and commit a full audit document (`docs/phase0_audit.md`) so Phase 1 blocks start from a known baseline.

**Architecture:** All work happens inside the `qemu-cxtg` submodule (path: `runtime-cxtg/qemu-cxtg`). A `feat/0.1` branch is cut from `qemu-cxtg:cxtg-dev`, and the audit + any fixups land there. All umbrella-repo (`runtime-cxtg`) changes (progress.md, todo.md, docs) happen on the umbrella's `cxtg-dev` branch.

**Tech Stack:** QEMU 9.x (RISC-V target), Meson/Ninja build system, C99, RISC-V ISA (RV64GC baseline). All shell commands assume CWD = `runtime-cxtg/qemu-cxtg` unless noted with `[UMBRELLA]`.

---

## Context / Pre-Read

### What Artur's commits added (already in `qemu-cxtg:cxtg-dev`)

| Commit (cxtg-dev hash) | What it does |
|---|---|
| `83a4000ef8` | Adds `ext_zicx` bool to `cpu_cfg_fields.h.inc`; resets cxsel/cxidx/cxdata to 0 in `cpu.c` |
| `109b8d5cde` | Adds `CSR_CXSEL=0x800` to `cpu_bits.h`; adds read/write handlers in `csr.c`; cxsel is URO (writes return `RISCV_EXCP_ILLEGAL_INST`) |
| `ccdc43197d` | `[DEBUG]` trace feature string in `cpu.c` |
| `df1458f00d` | Adds CLAUDE.md to `qemu-cxtg` |
| `546e86a1c3` | Adds `CSR_CXSIDX=0x801`, `CSR_CXSDATA=0x802`, adds cxsetsel as a *CSR* (incorrectly), adds `cxidx`/`cxdata` fields to `CPURISCVState`, adds `cx.c`/`cx.h` |
| `9de82f5a36` | Adds `trace-events` entries for all three CSRs |
| `6f9f35bd0f` | Wires read/write handlers in `cx.c` to `env->cxidx` and `env->cxdata` |
| `02ae7f935a` | **Removes cxsetsel as a CSR** (correct — it is an instruction), fixes typos; final state: 3 CSRs only |
| `2afb472f9d` | Adds `Composable-Extensions.md` to repo root |
| `a279c0ff59` | Adds `spec.md` to repo root |

### Known discrepancies vs. action plan (record these in audit)

| Issue | Artur's value | Action plan value | Notes |
|---|---|---|---|
| cxsel CSR address | `0x800` | `0xCC0` | Spec says 0xCC0; this needs a TODO |
| Extension name | `zicx` | `zcx` | Minor naming; record as TODO |
| State field names | `cxidx`, `cxdata` | `cxsidx`, `cxsdata` | Minor; leave as-is for now |
| cxsetsel | Not in `insn32.decode` | Should be instruction | Scope for Block 1.2 |
| scxstp | Absent | Phase 2 | Expected — not yet |
| DT CXU registration | None | Phase 5.3 | Expected — not yet |

### `feature/cx` vs `cxtg-dev` branch relationship

`cxtg-dev` already has cherry-picked versions of all 10 of Artur's commits — confirmed by matching commit message fingerprints. The `feature/cx` branch has those same commits on top of a **newer QEMU upstream** (more recent block/i386 commits). The action plan says to retire `feature/cx` after merge; since the CX content is already in `cxtg-dev`, this amounts to verifying completeness and then deleting the branch.

---

## File Map

| File | Create/Modify | Responsibility |
|---|---|---|
| `qemu-cxtg/target/riscv/cpu_bits.h` | Modify | Ensure named constants for all CX CSR addresses; add `// TODO: address TBD` markers |
| `qemu-cxtg/target/riscv/cpu.h` | Read-only verify | Confirm cxsel/cxidx/cxdata fields present |
| `qemu-cxtg/target/riscv/csr.c` | Read-only verify | Confirm CSR table entries and privilege levels |
| `qemu-cxtg/target/riscv/cx.c` | Read-only verify | Confirm read/write logic |
| `qemu-cxtg/target/riscv/insn32.decode` | Read-only verify | Confirm cxsetsel absent (expected gap for Block 1.2) |
| `qemu-cxtg/CLAUDE.md` | Read-only | Reference for build commands |
| `runtime-cxtg/docs/phase0_audit.md` | **Create** | Audit findings: DT schema, CSR inventory, cxsetsel status, discrepancies |
| `runtime-cxtg/docs/flowchart_direct_mode.svg` | **Create** | Direct mode flowchart (see Task 6) |
| `runtime-cxtg/docs/todo.md` | Modify | Add entries for TBD CSR addresses |
| `runtime-cxtg/docs/progress.md` | Modify | Mark Block 0.1 complete |

---

## Task 1: Cut feat/0.1 branch

**Files:** `qemu-cxtg` git state

- [ ] **Step 1.1: Verify you are in the qemu-cxtg submodule**

```bash
cd runtime-cxtg/qemu-cxtg
git status
git branch
```

Expected: on branch `cxtg-dev` (or `feature/cx`). No uncommitted changes.

- [ ] **Step 1.2: Ensure cxtg-dev is up to date**

```bash
git checkout cxtg-dev
git log --oneline -5
```

Expected: the five most recent commits include Artur's `target/riscv: add Claude Code spec.md` at the top.

- [ ] **Step 1.3: Cut feat/0.1**

```bash
git checkout -b feat/0.1
git log --oneline -3
```

Expected: branch `feat/0.1` checked out; same HEAD as `cxtg-dev`.

---

## Task 2: Verify Artur's commits are complete

**Files:** `qemu-cxtg/target/riscv/cpu_bits.h`, `cpu.h`, `csr.c`, `cx.c`, `cx.h`

- [ ] **Step 2.1: Confirm all 10 CX commits are present**

```bash
git log --oneline --author="[Aa]rtur" cxtg-dev | wc -l
git log --oneline --author="[Aa]rtur" cxtg-dev
```

Expected: 10 commits (Zicx extension, add CSR cxsel, DEBUG trace, CLAUDE.md, add cxsetsel+cxidx+cxdata, tracing, read/write wiring, remove cxsetsel CSR, Composable-Extensions.md, spec.md).

- [ ] **Step 2.2: Verify CSRs in cpu_bits.h**

```bash
grep -n "CSR_CX\|CXSEL_\|CSR_CXSIDX\|CSR_CXSDATA" target/riscv/cpu_bits.h
```

Expected output (exactly these defines exist):
```
#define CSR_CXSEL           0x800
#define CXSEL_INV           0x80000000
#define CXSEL_TYPE          0x70000000
#define CXSEL_SEL           0x0FFFFFFF
#define CSR_CXSIDX          0x801
#define CSR_CXSDATA         0x802
```

- [ ] **Step 2.3: Verify CPU state fields**

```bash
grep -n "cxsel\|cxidx\|cxdata" target/riscv/cpu.h
```

Expected: three `target_ulong` fields named `cxsel`, `cxidx`, `cxdata` in `CPURISCVState`.

- [ ] **Step 2.4: Verify CSR table registrations in csr.c**

```bash
grep -n "CSR_CXSEL\|CSR_CXSIDX\|CSR_CXSDATA\|\"cxsel\"\|\"cxsidx\"\|\"cxsdata\"" target/riscv/csr.c
```

Expected: three entries in the CSR table (`csr_ops[]`), one per CSR.

- [ ] **Step 2.5: Confirm cxsetsel is NOT a CSR (it was correctly removed)**

```bash
grep -n "cxsetsel" target/riscv/csr.c
```

Expected: zero matches. If any appear, Artur's removal commit was not applied.

- [ ] **Step 2.6: Confirm cxsetsel is NOT yet an instruction**

```bash
grep -in "cxsetsel" target/riscv/insn32.decode target/riscv/translate.c 2>/dev/null
```

Expected: zero matches. This is the expected gap for Block 1.2.

---

## Task 3: Build verification

**Files:** `qemu-cxtg/build/` (existing build directory)

- [ ] **Step 3.1: Rebuild from the existing build directory**

```bash
cd build
ninja -j$(nproc) qemu-system-riscv64 2>&1 | tail -20
```

Expected: `ninja: no work to do.` or a clean build with `[100%]` line and no errors. If there are warnings on CX files, note them.

- [ ] **Step 3.2: If build fails, run full configure + build**

Only do this step if Step 3.1 fails:
```bash
cd runtime-cxtg/qemu-cxtg
mkdir -p build && cd build
../configure --target-list=riscv64-softmmu 2>&1 | tail -10
ninja -j$(nproc) qemu-system-riscv64 2>&1 | tail -20
```

Expected: exits 0.

- [ ] **Step 3.3: Smoke-test the binary**

```bash
runtime-cxtg/qemu-cxtg/build/qemu-system-riscv64 --version
```

Expected: prints QEMU version and exits 0.

- [ ] **Step 3.4: Confirm CX CSR entries compile in**

```bash
grep -c "cxsel\|cxsidx\|cxsdata" runtime-cxtg/qemu-cxtg/build/qemu-system-riscv64 2>/dev/null || \
strings runtime-cxtg/qemu-cxtg/build/qemu-system-riscv64 | grep -i "cxsel" | head -5
```

Expected: `cxsel` appears in the binary strings (CSR name strings are embedded).

---

## Task 4: Ubuntu boot test

**Files:** none (runtime verification only)

> **Note:** This step requires an Ubuntu disk image. If no image is available, skip to Task 5 and note "Ubuntu boot test skipped — no image available" in the audit. The bare-metal ELF tests used in Phase 1+ do NOT need Ubuntu.

- [ ] **Step 4.1: Locate Ubuntu RISC-V image (if available)**

```bash
ls ~/images/ 2>/dev/null || ls /var/lib/libvirt/images/ 2>/dev/null || find ~ -name "*.img" -o -name "*.qcow2" 2>/dev/null | grep -i "riscv\|ubuntu" | head -5
```

If no image found: skip to Task 5, note in audit.

- [ ] **Step 4.2: Boot to login prompt (5-minute timeout)**

```bash
timeout 300 runtime-cxtg/qemu-cxtg/build/qemu-system-riscv64 \
  -M virt -m 2G -smp 2 \
  -kernel <path-to-kernel> \
  -drive file=<path-to-image>,format=qcow2 \
  -nographic -no-reboot 2>&1 | grep -E "login:|panic|Oops|error" | head -20
```

Replace `<path-to-kernel>` and `<path-to-image>` with actual paths found in Step 4.1.

Expected: `login:` appears in output. No kernel panic or QEMU crash.

---

## Task 5: Audit — answer all required questions

**Files:** `qemu-cxtg/target/riscv/csr.c`, `cpu_bits.h`, `cpu.h`, `cx.c`, `cx.h`, `insn32.decode`, `hw/riscv/`

- [ ] **Step 5.1: Audit DT / CXU registration**

```bash
grep -rn "cx\|CX\|composable" runtime-cxtg/qemu-cxtg/hw/riscv/ 2>/dev/null | grep -iv "\.pyc" | head -30
```

Expected: zero results (no DT CXU registration exists yet). Record finding: "No DT CXU registration present; deferred to Block 5.3."

- [ ] **Step 5.2: Full CSR inventory**

```bash
grep -n "CSR_CX\|cxsel\|cxsidx\|cxsdata\|cxidx\|cxdata" \
  runtime-cxtg/qemu-cxtg/target/riscv/cpu_bits.h \
  runtime-cxtg/qemu-cxtg/target/riscv/cpu.h \
  runtime-cxtg/qemu-cxtg/target/riscv/csr.c
```

Record: address, privilege (URO/URW), reset value, read/write handler behavior for each CSR.

- [ ] **Step 5.3: Verify privilege levels in csr.c predicate functions**

```bash
grep -A 10 "static RISCVException cxsel\b\|static RISCVException cxsidx\b\|static RISCVException cxsdata\b" \
  runtime-cxtg/qemu-cxtg/target/riscv/csr.c
```

These are the predicate functions (access-check). Record what privilege levels are required. The plan expects URO for cxsel (user read-only), URW for cxsidx/cxsdata.

- [ ] **Step 5.4: cxsetsel instruction status**

```bash
grep -rn "cxsetsel\|CXSETSEL\|cx_setsel" \
  runtime-cxtg/qemu-cxtg/target/riscv/ 2>/dev/null
```

Expected: zero results. Record: "cxsetsel not present as instruction; implement in Block 1.2."

- [ ] **Step 5.5: Scan for any other CX logic beyond the three CSRs**

```bash
grep -rn "scxstp\|scxxs\|cxdiscard\|SCXSTP\|SCXXS\|ZcxMulti\|zcxmulti" \
  runtime-cxtg/qemu-cxtg/target/riscv/ 2>/dev/null | head -20
```

Expected: zero results. Record: "No Phase 2+ CSRs present; all are new work."

- [ ] **Step 5.6: Check for debug printf statements to clean up**

```bash
grep -n "qemu_printf\|printf.*Zicx\|printf.*CX\|printf.*cx" \
  runtime-cxtg/qemu-cxtg/target/riscv/cpu.c | head -10
```

Expected: finds `qemu_printf("# Zicx SET!")` etc. These are debug noise. Note them in audit; they can be removed in Block 1.1 or as a fixup commit here.

- [ ] **Step 5.7: Check extension name for spec alignment**

```bash
grep -n "zicx\|ext_zicx\|Zicx\|zcx\|ext_zcx" \
  runtime-cxtg/qemu-cxtg/target/riscv/cpu.c \
  runtime-cxtg/qemu-cxtg/target/riscv/cpu_cfg_fields.h.inc 2>/dev/null
```

Record extension name actually used (`zicx`). Note that the action plan uses `zcx` — record as open question in todo.md: "Confirm correct ISA extension name with spec (current: zicx)."

---

## Task 6: Optional fixup — remove debug printfs

**Files:** `qemu-cxtg/target/riscv/cpu.c`

Only perform this task if Step 5.6 found debug `qemu_printf` calls inside the zicx initialization block.

- [ ] **Step 6.1: Read the relevant section of cpu.c**

```bash
grep -n "Zicx\|Zicx SET\|Zicx NOT" runtime-cxtg/qemu-cxtg/target/riscv/cpu.c
```

Note the line numbers.

- [ ] **Step 6.2: Remove the debug printf lines**

Open `target/riscv/cpu.c`, find the block that looks like:
```c
if (riscv_cpu_cfg(env)->ext_zicx) {
    env->cxsel = 0;
    env->cxidx = 0;
    env->cxdata = 0;
    qemu_printf("# Zicx SET! [HART=%d]\n", (int)env->mhartid);
```
and:
```c
    qemu_printf("# Zicx NOT SET! [HART=%d]\n", (int)env->mhartid);
```

Remove only the two `qemu_printf` lines. Keep the `env->cx*` resets.

- [ ] **Step 6.3: Rebuild to verify no new errors**

```bash
cd runtime-cxtg/qemu-cxtg/build
ninja -j$(nproc) qemu-system-riscv64 2>&1 | tail -5
```

Expected: clean build.

- [ ] **Step 6.4: Commit the cleanup**

```bash
cd runtime-cxtg/qemu-cxtg
git add target/riscv/cpu.c
git commit -m "target/riscv: remove debug printf statements from zicx init"
```

---

## Task 7: Add TODO markers for TBD CSR addresses

**Files:** `qemu-cxtg/target/riscv/cpu_bits.h`

- [ ] **Step 7.1: Read the current CX section in cpu_bits.h**

Open [qemu-cxtg/target/riscv/cpu_bits.h](qemu-cxtg/target/riscv/cpu_bits.h) and find the CX Extension block (around `CSR_CXSEL`).

- [ ] **Step 7.2: Add TODO comments per the hardcoded-address rule**

The action plan (section: "Hardcoded address rule") requires every hardcoded address have a `// TODO: address TBD` comment. Edit `cpu_bits.h` — the CX block should read:

```c
/* CX Extension */
/* TODO: address TBD — pending spec finalisation; spec draft says 0xCC0, Artur used 0x800 */
#define CSR_CXSEL           0x800

/* CXSEL CSR bits */
#define CXSEL_INV           0x80000000
#define CXSEL_TYPE          0x70000000
#define CXSEL_SEL           0x0FFFFFFF

/* TODO: address TBD — pending spec finalisation */
#define CSR_CXSIDX          0x801
/* TODO: address TBD — pending spec finalisation */
#define CSR_CXSDATA         0x802
```

- [ ] **Step 7.3: Rebuild to verify**

```bash
cd runtime-cxtg/qemu-cxtg/build && ninja -j$(nproc) qemu-system-riscv64 2>&1 | tail -3
```

Expected: clean.

- [ ] **Step 7.4: Commit**

```bash
cd runtime-cxtg/qemu-cxtg
git add target/riscv/cpu_bits.h
git commit -m "target/riscv: add TODO markers for hardcoded CX CSR addresses"
```

---

## Task 8: Write docs/phase0_audit.md

**Files:** `runtime-cxtg/docs/phase0_audit.md` (create in umbrella repo)

- [ ] **Step 8.1: Create the audit document**

Create the file at `[docs/phase0_audit.md](docs/phase0_audit.md)` in the umbrella repo (`runtime-cxtg/`). Populate it with findings from Tasks 2 and 5. Template:

```markdown
# Phase 0 Audit — Block 0.1

**Date:** 2026-05-19  
**Branch audited:** `qemu-cxtg:cxtg-dev` (feat/cx integration)  
**Auditor:** <your name>

## 1. Branch Integration Status

Artur's `feature/cx` commits are fully cherry-picked into `cxtg-dev`.
Verified 10 commits present (list hashes and messages here).

## 2. Device Tree CXU Registration

**Finding:** None. No DT node generation code for CX exists anywhere in `hw/riscv/`.

**What needs to change for Block 5.3:**
- Add `cx_dt_info_t` struct to `cx.h`
- Emit `/cx/<name>` DT nodes from machine model in `hw/riscv/virt.c`
- Schema: `compatible = "riscv,cx-v1"; uuid; cxid; num_contexts; state_words`

## 3. CSR Inventory

| CSR | Address | Privilege | Reset | R/W Handler |
|-----|---------|-----------|-------|-------------|
| cxsel   | 0x800 | URO | 0 | read: returns `env->cxsel`; write: returns `RISCV_EXCP_ILLEGAL_INST` |
| cxsidx  | 0x801 | URW | 0 | read/write `env->cxidx`; no WARL enforcement yet |
| cxsdata | 0x802 | URW | 0 | read/write `env->cxdata`; no auto-increment yet |

**Cross-check against plan:**
- Expected: cxsel (0xCC0), cxsidx (0x801), cxsdata (0x802) — **cxsel address mismatch** (0x800 vs 0xCC0)
- cxsidx WARL (valid range clamp) not yet implemented — Block 1.1 scope
- cxsdata auto-increment not yet implemented — Block 4.2 scope

## 4. cxsetsel Status

**Finding:** `cxsetsel` was initially added as a CSR and then correctly **removed** (commit `02ae7f93`). It is NOT present in `insn32.decode` or `translate.c`.

**Implication for Block 1.2:** Full implementation required — encoding (SYSTEM opcode, funct3 TBD), translate.c handler, `utils.h` macro.

## 5. Other CX Logic

**scxstp:** Absent. New work in Block 2.1.  
**scxxs0-3:** Absent. New work in Block 3.1.  
**cxdiscard / scxdiscard:** Absent. New work in Block 4.3.  
**Indirect mode table walk:** Absent. Phase 6.  
**Stateen bits:** Absent. Phase 7.

## 6. Discrepancies and Open Questions

| # | Symbol | Current Value | Plan/Spec Value | Action |
|---|--------|--------------|-----------------|--------|
| 1 | CSR_CXSEL address | 0x800 | 0xCC0 (spec draft) | TODO in cpu_bits.h; resolve when spec finalised |
| 2 | Extension name | `zicx` | `zcx` | Confirm correct name with spec owners |
| 3 | CPURISCVState field | `cxidx`, `cxdata` | `cxsidx`, `cxsdata` | Minor; leave for now, rename in bulk later |
| 4 | cxsetsel instruction | Not present | Block 1.2 | Expected gap |
| 5 | Debug printf in cpu.c | Present | Should be removed | Done (Task 6) or note if skipped |

## 7. Build Status

- Binary: `qemu-cxtg/build/qemu-system-riscv64`
- Build result: [PASS / FAIL — fill in]
- `--version` exits 0: [YES / NO]
- Ubuntu boot: [PASS / SKIPPED — reason]
- Compile warnings on CX files: [list any, or NONE]

## 8. Phase 1 Block Scope Adjustments

Based on this audit, no Phase 1 blocks are reduced in scope — Artur's work is limited to CSR stubs with no behavioral wiring. All Phase 1 blocks remain as written.
```

- [ ] **Step 8.2: Fill in all placeholder fields with actual findings from Tasks 2–5**

Replace every `[fill in]`, `[list any]`, `[YES/NO]`, `<your name>` with the real data observed.

- [ ] **Step 8.3: Commit the audit document to the umbrella repo**

```bash
cd runtime-cxtg
git add docs/phase0_audit.md
git commit -m "docs: add phase0_audit.md — Block 0.1 audit findings"
```

---

## Task 9: Draw direct-mode flowchart

**Files:** `runtime-cxtg/docs/flowchart_direct_mode.svg`

The flowchart shows the Direct mode execution path based on the actual code. Since cxsetsel is not yet implemented, the diagram documents the *planned* path (with "TBD" for the cxsetsel node).

- [ ] **Step 9.1: Create the SVG flowchart**

Create `[docs/flowchart_direct_mode.svg](docs/flowchart_direct_mode.svg)` with the following content. This is valid SVG — paste it verbatim:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<svg width="520" height="680" xmlns="http://www.w3.org/2000/svg" font-family="monospace" font-size="12">
  <title>Zcx Direct Mode Execution Flow (Phase 0 baseline)</title>

  <!-- Start -->
  <ellipse cx="260" cy="30" rx="80" ry="18" fill="#c8e6c9" stroke="#388e3c" stroke-width="1.5"/>
  <text x="260" y="35" text-anchor="middle">Hart executes CX insn</text>

  <!-- Arrow -->
  <line x1="260" y1="48" x2="260" y2="78" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>

  <!-- cxsetsel -->
  <rect x="160" y="78" width="200" height="36" rx="4" fill="#fff9c4" stroke="#f9a825" stroke-width="1.5"/>
  <text x="260" y="99" text-anchor="middle">cxsetsel rd, rs1</text>
  <text x="260" y="110" text-anchor="middle" font-size="10" fill="#888">(TBD — Block 1.2)</text>

  <line x1="260" y1="114" x2="260" y2="144" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>

  <!-- scxstp check -->
  <polygon points="260,144 390,178 260,212 130,178" fill="#e3f2fd" stroke="#1976d2" stroke-width="1.5"/>
  <text x="260" y="182" text-anchor="middle">scxstp.mode?</text>
  <text x="260" y="193" text-anchor="middle" font-size="10" fill="#888">(TBD — Block 2.1)</text>

  <!-- Disabled branch -->
  <line x1="130" y1="178" x2="60" y2="178" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="95" y="172" text-anchor="middle" font-size="10">Disabled</text>
  <rect x="10" y="188" width="100" height="28" rx="4" fill="#ffcdd2" stroke="#c62828" stroke-width="1.5"/>
  <text x="60" y="207" text-anchor="middle">Illegal Inst</text>

  <!-- Direct branch -->
  <line x1="260" y1="212" x2="260" y2="242" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="280" y="230" font-size="10">Direct</text>

  <!-- cxsel check -->
  <polygon points="260,242 390,276 260,310 130,276" fill="#e3f2fd" stroke="#1976d2" stroke-width="1.5"/>
  <text x="260" y="276" text-anchor="middle" font-size="11">cxsel == CX_SEL_INVALID?</text>
  <text x="260" y="290" text-anchor="middle" font-size="10" fill="#888">(TBD — Block 2.3)</text>

  <!-- Yes branch -->
  <line x1="390" y1="276" x2="460" y2="276" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="425" y="270" text-anchor="middle" font-size="10">Yes (~0UL)</text>
  <rect x="460" y="262" width="50" height="28" rx="4" fill="#ffcdd2" stroke="#c62828" stroke-width="1.5"/>
  <text x="485" y="281" text-anchor="middle">Trap</text>

  <!-- No branch -->
  <line x1="260" y1="310" x2="260" y2="340" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="280" y="328" font-size="10">No</text>

  <!-- cxsel=0 check -->
  <polygon points="260,340 370,368 260,396 150,368" fill="#e3f2fd" stroke="#1976d2" stroke-width="1.5"/>
  <text x="260" y="372" text-anchor="middle" font-size="11">cxsel == 0?</text>

  <!-- cxsel=0: builtin -->
  <line x1="150" y1="368" x2="80" y2="368" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="115" y="362" text-anchor="middle" font-size="10">Yes</text>
  <rect x="10" y="354" width="70" height="28" rx="4" fill="#c8e6c9" stroke="#388e3c" stroke-width="1.5"/>
  <text x="45" y="373" text-anchor="middle">Builtin CX</text>

  <!-- Extract CXID+IDX -->
  <line x1="260" y1="396" x2="260" y2="426" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="280" y="414" font-size="10">No</text>
  <rect x="155" y="426" width="210" height="30" rx="4" fill="#fff9c4" stroke="#f9a825" stroke-width="1.5"/>
  <text x="260" y="446" text-anchor="middle">Extract CXID=cxsel[15:8], IDX=cxsel[7:0]</text>

  <line x1="260" y1="456" x2="260" y2="486" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>

  <!-- CX registered? -->
  <polygon points="260,486 380,514 260,542 140,514" fill="#e3f2fd" stroke="#1976d2" stroke-width="1.5"/>
  <text x="260" y="518" text-anchor="middle" font-size="11">CXID registered?</text>

  <!-- Not registered -->
  <line x1="380" y1="514" x2="450" y2="514" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="415" y="508" text-anchor="middle" font-size="10">No</text>
  <rect x="450" y="500" width="50" height="28" rx="4" fill="#ffcdd2" stroke="#c62828" stroke-width="1.5"/>
  <text x="475" y="519" text-anchor="middle">Trap</text>

  <!-- Dispatch -->
  <line x1="260" y1="542" x2="260" y2="572" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="280" y="560" font-size="10">Yes</text>
  <rect x="155" y="572" width="210" height="30" rx="4" fill="#c8e6c9" stroke="#388e3c" stroke-width="1.5"/>
  <text x="260" y="592" text-anchor="middle">Dispatch to CX[CXID].execute()</text>

  <line x1="260" y1="602" x2="260" y2="632" stroke="#555" stroke-width="1.5" marker-end="url(#arr)"/>
  <ellipse cx="260" cy="650" rx="80" ry="18" fill="#c8e6c9" stroke="#388e3c" stroke-width="1.5"/>
  <text x="260" y="655" text-anchor="middle">Return result to rd</text>

  <!-- Arrow marker -->
  <defs>
    <marker id="arr" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
      <path d="M0,0 L0,6 L8,3 z" fill="#555"/>
    </marker>
  </defs>
</svg>
```

- [ ] **Step 9.2: Verify the SVG renders**

Open the file in a browser or SVG viewer and confirm it shows a flowchart with labeled nodes. If your environment has `inkscape` or `eog`:
```bash
eog runtime-cxtg/docs/flowchart_direct_mode.svg 2>/dev/null || \
xdg-open runtime-cxtg/docs/flowchart_direct_mode.svg 2>/dev/null || \
echo "Open docs/flowchart_direct_mode.svg manually to verify rendering"
```

- [ ] **Step 9.3: Commit**

```bash
cd runtime-cxtg
git add docs/flowchart_direct_mode.svg
git commit -m "docs: add direct mode flowchart (Phase 0 baseline)"
```

---

## Task 10: Update todo.md and progress.md

**Files:** `runtime-cxtg/docs/todo.md`, `runtime-cxtg/docs/progress.md`

- [ ] **Step 10.1: Read current todo.md**

Open [docs/todo.md](docs/todo.md) to see its current format.

- [ ] **Step 10.2: Add entries for TBD CSR addresses**

Append entries to `docs/todo.md`:

```markdown
## CSR Address Stubs (Phase 0)

| Symbol | File | Current Value | Pending |
|--------|------|---------------|---------|
| `CSR_CXSEL` | `qemu-cxtg/target/riscv/cpu_bits.h` | `0x800` | Spec draft says `0xCC0`; confirm with spec owners |
| `CSR_CXSIDX` | `qemu-cxtg/target/riscv/cpu_bits.h` | `0x801` | Pending spec finalisation |
| `CSR_CXSDATA` | `qemu-cxtg/target/riscv/cpu_bits.h` | `0x802` | Pending spec finalisation |
| Extension name | `qemu-cxtg/target/riscv/cpu.c` etc. | `zicx` | Confirm correct ISA name (`zcx` vs `zicx`) |
```

- [ ] **Step 10.3: Read current progress.md**

Open [docs/progress.md](docs/progress.md) to see its format.

- [ ] **Step 10.4: Mark Block 0.1 complete in progress.md**

Add or update the Block 0.1 entry to reflect completion:

```markdown
## Phase 0

### Block 0.1 — Integrate Artur's branch, audit, build, and boot ✅

- Branch integration: feat/cx cherry-picked into cxtg-dev ✅
- Clean build: qemu-system-riscv64 builds without errors ✅
- Ubuntu boot: [PASS / SKIPPED]
- Audit doc: docs/phase0_audit.md committed ✅
- Flowchart: docs/flowchart_direct_mode.svg committed ✅
- feat/cx retired: [YES / PENDING]
```

- [ ] **Step 10.5: Commit**

```bash
cd runtime-cxtg
git add docs/todo.md docs/progress.md
git commit -m "docs: update progress and todo for Block 0.1 completion"
```

---

## Task 11: Merge feat/0.1 → cxtg-dev in qemu-cxtg; retire feature/cx

**Files:** `qemu-cxtg` git state

- [ ] **Step 11.1: Merge feat/0.1**

```bash
cd runtime-cxtg/qemu-cxtg
git checkout cxtg-dev
git merge --no-ff feat/0.1 -m "feat: Block 0.1 — integrate audit, build verification, TODO markers"
```

Expected: clean merge, no conflicts (feat/0.1 is a linear extension of cxtg-dev).

- [ ] **Step 11.2: Delete feat/0.1**

```bash
git branch -d feat/0.1
```

- [ ] **Step 11.3: Delete feature/cx (retired per plan)**

The action plan says to delete `feat/cx` after integration. Since the CX commits are already in `cxtg-dev`, delete it:

```bash
git branch -d feature/cx 2>/dev/null || echo "Already deleted or not local"
git push origin --delete feature/cx 2>/dev/null || echo "No remote to push to"
```

- [ ] **Step 11.4: Verify final state**

```bash
git log --oneline -5
git branch
```

Expected: `cxtg-dev` is HEAD; `feat/0.1` and `feature/cx` are gone; last 5 commits include your fixup and audit work.

---

## Task 12: Final verification

- [ ] **Step 12.1: One last clean build**

```bash
cd runtime-cxtg/qemu-cxtg/build
ninja -j$(nproc) qemu-system-riscv64 2>&1 | tail -5
./qemu-system-riscv64 --version
```

Expected: version printed, exit 0.

- [ ] **Step 12.2: grep confirms CX CSR entries in csr.c**

```bash
grep -c "cxsel\|cxsidx\|cxsdata" runtime-cxtg/qemu-cxtg/target/riscv/csr.c
```

Expected: ≥ 6 (two function names per CSR × 3 CSRs).

- [ ] **Step 12.3: Confirm audit and flowchart are committed**

```bash
ls runtime-cxtg/docs/phase0_audit.md
ls runtime-cxtg/docs/flowchart_direct_mode.svg
git -C runtime-cxtg log --oneline -8
```

Expected: both files exist; commits appear in log.

---

## Definition of Done Checklist

- [ ] `feature/cx` retired (deleted)
- [ ] Clean build: `qemu-system-riscv64 --version` exits 0
- [ ] Ubuntu boot OR documented reason why skipped
- [ ] `docs/phase0_audit.md` committed with: DT schema finding, CSR inventory table, cxsetsel status, discrepancy table
- [ ] `docs/flowchart_direct_mode.svg` committed
- [ ] `docs/todo.md` updated with CSR address TBDs
- [ ] `docs/progress.md` updated marking Block 0.1 complete
- [ ] All changes in `qemu-cxtg` landed via `feat/0.1` → `cxtg-dev`
- [ ] Phase 1 block scopes confirmed as-written (no reduction warranted)
