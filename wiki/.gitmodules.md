# .gitmodules

Submodule configuration for `runtime-cxtg`.

**Phase 0:** created

## Submodules registered

- `qemu-cxtg` — QEMU fork implementing the Zcx/ZcxMulti hardware model
- `linux-cxtg` — Kernel for S-mode testing (future work)