# target/riscv/cpu_bits.h — CXSEL_TYPE

bitmask: extension type field in the `cxsel` CSR; bits 30:28; encodes the category of the selected composable extension

```c
#define CXSEL_TYPE          0x70000000
```
